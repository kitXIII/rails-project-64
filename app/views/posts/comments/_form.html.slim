- if user_signed_in?
  = simple_form_for(comment, url: post_comments_path(post)) do |f|
    = f.input :content, label: false, error: false, hint: false, wrapper_class: 'mb-2'

    = f.button :submit, class: 'btn-primary'
